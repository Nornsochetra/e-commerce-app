spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/e-commerce}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:1234}
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
